import React from 'react';
import { FlexLink } from 'components';

export const BackToProductsBtn = () => (
  <FlexLink text={'Forgot something?'} to={'/products/search'}>
    Back to products
  </FlexLink>
);

